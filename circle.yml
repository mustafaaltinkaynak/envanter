machine:
  timezone:
    Europe/Istanbul
  ruby:
    version: "2.3"
dependencies:
  pre:
    - gem update --system
test:
  pre:
    - bundle exec rubocop --rails --display-cop-names
  override:
    - "true"
